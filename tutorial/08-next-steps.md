# Next Steps

You now have the basics. To go deeper:

- Browse `examples/` for real programs (arrays, iteration, operator methods, I/O).
- Read the language specification: `docs/vexel-rfc.md`.
- Explore compiler/backend internals via the source map in `README.md`.
- Check backend structure in `backends/` (`backends/c` is the reference).
- Check testing harnesses in `frontend/Makefile`, `backends/c/tests/run_tests.py`, and `backends/tests/run_conformance.sh`.

If you want a printable or single-page version of this tutorial, open an issue or ask for a combined document.

- Prev: [Modules and Resources](07-modules-and-resources.md)
- Next: (end)
