// @rfc: docs/vexel-rfc.md#expressions--control
// @desc: Method calls pass the receiver by reference for the duration of the call | Method calls pass the receiver by reference for the duration of the call


// EX-007: Receiver passed by reference
#Counter(
    value:#i32
);

&(self)#Counter::increment() {
    self.value = self.value + 1;
}

&^main() -> #i32 {
    c:#Counter = Counter(0);
    c.increment();
    c.increment();
    -> c.value;  // Should be 2
}
