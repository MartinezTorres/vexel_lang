// @rfc: doc/vexel-rfc.md#expressions--control
// @desc: When condition is a runtime value, both branches must have the same type | Runtime conditional with different branch types is an error


// EX-017: Runtime conditional type mismatch error
&^main() -> #i32 {
    x:#i32 = 10;
    // ERROR: runtime condition but branches have different types
    result = x > 5 ? 100 : "error";
    -> 0;
}
