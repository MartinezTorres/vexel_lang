// @rfc: doc/vexel-rfc.md#expressions--control
// @desc: When condition is a runtime value, both branches must have the same type | When condition is a runtime value, both branches must have the same type


// EX-017: Runtime conditionals need same type
&^main() -> #i32 {
    x:#i32 = 10;
    // x > 5 is runtime condition, both branches must have same type
    result:#i32 = x > 5 ? 100 : 200;
    -> result;
}
