&!ua() -> #u32.32;
&!ub() -> #u32.32;

&^main() -> #i32 {
  u:#u32.32 = ua();
  v:#u32.32 = ub();

  p:#u32.32 = u * v;
  d:#u32.32 = p / v;
  m:#u32.32 = p % v;

  c:#u32.32 = u;
  c *= v;
  c /= v;
  c %= v;

  ok:#b = (d == u) && (c == u) && (m < v);
  ok ? 0 : 1
}
