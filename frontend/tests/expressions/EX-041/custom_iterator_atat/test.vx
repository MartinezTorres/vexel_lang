// @rfc: docs/vexel-rfc.md#expressions--control
// @desc: Named types can override sorted iteration by defining &(self)#Type::@@($loop) method | Named types can override sorted iteration by defining &(self)#Type::@@($loop) method


// EX-041: Custom sorted iterator with @@
#ReverseRange(
    start:#i32,
    end:#i32
);

&(self)#ReverseRange::@@($loop) {
    _:#i32 = self.end - 1;
    $loop;
}

&print(arg0:#i32) { }

&^main() -> #i32 {
    r:#ReverseRange = ReverseRange(0, 5);
    r@@print(_);
    -> 0;
}
