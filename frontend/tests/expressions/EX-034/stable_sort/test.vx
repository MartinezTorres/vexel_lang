// @rfc: docs/vexel-rfc.md#expressions--control
// @desc: Backends sort with a stable algorithm before entering the @@ loop body | Backends sort with a stable algorithm before entering the @@ loop body


// EX-034: Stable sort algorithm
&!print(arg0:#i32);

&^main() -> #i32 {
    arr:#i32[6] = [3, 1, 2, 1, 3, 2];
    arr@@print(_);  // Stable sort maintains relative order of equal elements
    -> 0;
}
