// @rfc: docs/vexel-rfc.md#declarations
// @desc: Iterable expressions can be identifiers | Identifier iterable still works


// DC-085: Identifier iterable still works
#Iterable(count:#i32);

&(it)#Iterable::@($loop) {
    _:#i32 = it.count;
    $loop;
}

&^main() -> #i32 {
    obj:#Iterable = Iterable(3);
    sum:#i32 = 0;
    obj@{ sum = sum + _; }
    sum
}
