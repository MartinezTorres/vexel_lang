// @rfc: docs/vexel-rfc.md#lexical--tokens
// @desc: String literals have no length limit. | Accept string literals with no length limit (>200 chars)


&^main() -> #i32 {
    s: #s = "This is a very long string literal that should be accepted by the lexer because there is no length limit according to the specification and we need to verify that it works correctly even with extremely long strings like this one that goes on and on and on and on"
    0
}
