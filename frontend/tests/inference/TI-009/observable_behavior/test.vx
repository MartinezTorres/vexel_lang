// @rfc: doc/vexel-rfc.md#type-inference--generics
// @desc: Observable semantics must match a concrete instantiation regardless of implementation optimizations | Observable semantics match concrete instantiation


&compute(x, y) {
    x * 2 + y * 3
}

&^main() -> #i32 {
    compute(5, 7)
}
