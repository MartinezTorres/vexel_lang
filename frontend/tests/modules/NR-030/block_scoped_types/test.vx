// @rfc: doc/vexel-rfc.md#name-resolution--modules
// @desc: Type definitions can be scoped to blocks (visibility limited to that scope) | Type definitions can be scoped to blocks


// NR-030: Type definitions can be scoped to blocks (visibility limited to that scope)
&^main() -> #i32 {
    {
        #LocalType(x:#i32);
        val:#LocalType = LocalType(42);
    }
    // LocalType is not accessible here
    -> 0;
}
