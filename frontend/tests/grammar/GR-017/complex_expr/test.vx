// @rfc: doc/vexel-rfc.md#grammar
// @desc: Expressions are evaluated according to operator precedence, starting with assignment as the lowest precedence | Complex expression with all precedence levels


&^main() -> #i32 {
    a:#i32 = 1;
    b:#i32 = 2;
    c:#i32 = 3;
    // Ensures parser handles nested precedence without ambiguity
    a + b * c - (a + 1);
}
