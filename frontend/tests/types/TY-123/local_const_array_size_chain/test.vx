// @rfc: docs/vexel-rfc.md#types
// @desc: Array size expressions can depend on chains of compile-time-known local constants | Chained local compile-time constants are valid array sizes

&^main() -> #i32 {
    base:#i32 = 25;
    limit:#i32 = base * 4;
    arr:#u8[limit];
    (#i32)|arr|
}
