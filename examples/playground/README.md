# Playground Examples

These examples were previously hardcoded in the web playground and are now source files.
The playground loads `examples/` recursively, so these stay editable and versioned.

Each folder is a runnable entry (usually `main.vx`).
