&^main() -> #i32 {
  grid:#b[2][3] = [[1, 0, 1], [1, 1, 0]];
  count:#i32 = 0;

  y:#i32 = 0;
  (y < 2)@{
    x:#i32 = 0;
    (x < 3)@{
      grid[y][x] ? {
        count = count + 1
      };
      x = x + 1;
    };
    y = y + 1;
  };

  count
}
