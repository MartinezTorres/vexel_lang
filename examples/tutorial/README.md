# Tutorial Examples

This folder contains the curated examples used by the web playground and quick language walkthrough.

- `manifest.json` is the ordered tutorial index consumed by the playground.
- `quickadd.vx` is the default first tutorial step.
- `multifile/` demonstrates module imports across files.
- The rest are focused single-file examples for core language features.
