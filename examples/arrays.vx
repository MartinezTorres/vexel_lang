// Vexel Example: Arrays and ranges

// Array literal
arr:#i32[3] = [1, 2, 3];

// Range operator (should expand to array)
rng:#i32[5] = [0, 1, 2, 3, 4];

// Access array elements
&sum_array() -> #i32 {
    arr[0] + arr[1] + arr[2] + rng[4]
}

// Entry point
&^main() -> #i32 {
    sum_array()
}
