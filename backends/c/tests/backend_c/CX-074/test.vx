// @rfc: backends/c/README.md#functions--calling
// @desc: Mutable receiver lowering must not take addresses of folded literals. | By-ref receiver args stay lvalues
// @expect-exit: 12
// @run-generated: true
// @command: {VEXEL_C} -b c test.vx && ! rg -n "=[[:space:]]*&[0-9]+;" out.c


&(lhs, rhs)shift(delta:#i8) {
    lhs = lhs + delta;
    rhs = rhs + delta;
}

&^main() -> #i32 {
    mut x:#i32;
    mut y:#i32;
    x = 1;
    y = 5;
    (x, y).shift(3);
    x + y
}
