// @rfc: backends/c/README.md#code-generation-contract
// @desc: Exported immutable globals are declared in the header and non-static in the C file. | Exported immutable globals appear in header and are non-static
// @expect-exit: 0
// @command: python3 -c "import pathlib, re, subprocess, sys; subprocess.run([sys.argv[1], '-b', 'c', 'test.vx'], check=True); src = pathlib.Path('out.c').read_text(); hdr = pathlib.Path('out.h').read_text(); ok = re.search(r'\\bextern\\s+VX_CONSTEXPR\\s+int32_t\\s+vx_table\\b', src) and re.search(r'\\bextern\\s+VX_CONSTEXPR\\s+int32_t\\s+vx_table\\b', hdr); sys.exit(0 if ok else 1)" {VEXEL_C}

[[export]]
table:#i32 = 7;

&^main() -> #i32 {
    0
}
