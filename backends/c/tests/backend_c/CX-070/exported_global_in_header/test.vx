// @rfc: backends/c/README.md#code-generation-contract
// @desc: Immutable globals stay in the C translation unit and are not declared in the public header. | Immutable globals are internal to generated C file
// @expect-exit: 0
// @command: python3 -c "import pathlib, re, subprocess, sys; subprocess.run([sys.argv[1], '-b', 'c', 'test.vx'], check=True); src = pathlib.Path('out.c').read_text(); hdr = pathlib.Path('out.h').read_text(); ok = re.search(r'\\bVX_CONSTEXPR\\s+int32_t\\s+vx_table\\s*=\\s*7\\b', src) and not re.search(r'\\bvx_table\\b', hdr); sys.exit(0 if ok else 1)" {VEXEL_C}

table:#i32 = 7;

&^main() -> #i32 {
    table
}
