// @rfc: docs/vexel-rfc.md#runtime-semantics
// @desc: Logical OR operator (||) short-circuits evaluation | Logical OR operator (||) short-circuits evaluation
// @expect-exit: 0
// @run-generated: true
// @command: {VEXEL_C} test.vx


// RT-003: Logical OR operator (||) short-circuits evaluation
// If left operand is true, right operand should NOT be evaluated

evaluated:#b = 0;

&trigger() -> #b {
    evaluated = 1;
    0;
}

&^main() -> #i32 {
    // true || trigger() should not call trigger()
    1 || trigger();

    // evaluated should still be false
    evaluated ? 1 : 0
}
