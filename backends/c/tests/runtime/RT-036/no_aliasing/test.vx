// @rfc: docs/vexel-rfc.md#runtime-semantics
// @desc: Assignment always creates independent copy (no aliasing) | Assignment always creates independent copy (no aliasing)
// @expect-exit: 30
// @run-generated: true
// @command: {VEXEL_C} test.vx


// RT-036: Assignment always creates independent copy (no aliasing)

&^main() -> #i32 {
    x:#i32[2] = [10, 20];
    y:#i32[2] = x;  // Copy, not alias
    y[0] = 99;
    x[0] + x[1]  // Should be 10 + 20 = 30
}
