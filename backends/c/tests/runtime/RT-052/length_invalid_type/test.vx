// @rfc: doc/vexel-rfc.md#runtime-semantics
// @desc: Length operator |expr| rejects other operand types | Length operator |expr| rejects other operand types
// @expect-exit: 1
// @command: {VEXEL_C} test.vx


// RT-052: Length operator |expr| rejects other operand types (except arrays, strings, signed/unsigned ints)

&^main() -> #i32 {
    b #bool = true
    |b|  // Should fail - bool doesn't support length operator
}
