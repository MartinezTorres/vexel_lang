// @rfc: docs/vexel-rfc.md#preamble
// @desc: Vexel is an operator-based language where all operations use operators rather than keywords | Operator-based language - logical operations via operators
// @expect-exit: 1
// @run-generated: true
// @command: {VEXEL} -b c test.vx


// GE-005: Operator-based language
// Logical operations via operators

&^main() -> #i32 {
    x:#i32 = 15
    y:#i32 = 30

    // Boolean operators
    a:#b = x < 20 && y > 10
    b:#b = x > 20 || y < 50
    c:#b = !a

    // Result based on operator expressions
    a ? 1 : (b ? 2 : 3)
}
