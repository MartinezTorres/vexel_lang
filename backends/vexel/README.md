# Vexel Backend

`vexel` is a text backend that emits the lowered whole-program Vexel module.

- Output file: `<stem>.vx`
- Stdout: emits the same lowered text for snapshot-driven tests

This backend is primarily used as a deterministic frontend/pipeline oracle.
