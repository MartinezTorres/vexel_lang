# Vexel Backend

`vexel` is a text backend that emits the lowered, frontend-pruned Vexel module.

- Output file: `<stem>.vx`

This backend is primarily used as a deterministic frontend/pipeline oracle.
